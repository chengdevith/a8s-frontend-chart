apiVersion: v1
kind: Service
metadata:
  name: {{ include "a8s-frontend.fullname" . }}
  labels:
    {{- include "a8s-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "a8s-frontend.selectorLabels" . | nindent 4 }}
